(function () {
	'use strict';
	app.controller('inventoryOutputEditController', inventoryOutputEditController);
	inventoryOutputEditController.$inject = ['$scope', '$q', '$state', '$stateParams'];
	function inventoryOutputEditController($scope, $q, $state, $stateParams) {
		// models
	
		// functions
		function activate() { 
		}
		
		function save() {			
		}
		
		/* start */
		activate();
	}
})();