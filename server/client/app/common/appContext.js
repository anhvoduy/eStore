(function () {
    'use strict';
    app.factory('appContext', appContext);
    appContext.$inject = ['$q', '$rootScope'];
    function appContext($q, $rootScope) {
        // constructor
        var appContext = function () {
            
        }
		
		
        return new appContext;
    };
})();